# Working Examples

Run the mock route example:

```bash
mix run examples/route_decision_mock.exs
```

Run the incremental policy example:

```bash
mix run examples/policy_plan_live.exs
```

Both examples use generic fixture model refs. They are examples of policy
contracts, not bindings to a specific model family.
