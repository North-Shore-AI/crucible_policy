# Testing

The default suite is offline and deterministic. It validates completed-trace
routing, incremental token-boundary evaluation, energy fusion, active-control
fail-closed behavior, uncertainty combiners, and JSON round trips for decision
contracts.

Use `mix ci` before publishing or integrating the package.
